language: nix
nix: 2.0.4
before_install: nix-shell '<nixpkgs>' -A nix-prefetch-github --run ./scripts/bump-fixed-build.sh
deploy:
  - provider: script
    script: ./scripts/deploy.sh
    on:
      branch: master
